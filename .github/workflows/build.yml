name: Build

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  # ── Windows ─────────────────────────────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: windows-deps-${{ hashFiles('CMakeLists.txt') }}

      - name: Configure
        run: >
          cmake -B build
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded

      - name: Build
        run: cmake --build build --config Release

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: RTSChess-Windows
          path: build/Release/RTSChess.exe

  # ── macOS arm64 (Apple Silicon) ──────────────────────────────────────────────
  build-macos-arm64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: macos-arm64-deps-${{ hashFiles('CMakeLists.txt') }}

      - name: Configure
        run: >
          cmake -B build
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_POLICY_VERSION_MINIMUM=3.5

      - name: Build
        run: cmake --build build --config Release

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: RTSChess-arm64
          path: build/RTSChess

  # ── macOS x86_64 (Intel) ─────────────────────────────────────────────────────
  build-macos-x86_64:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: macos-x86_64-deps-${{ hashFiles('CMakeLists.txt') }}

      - name: Configure
        run: >
          cmake -B build
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_POLICY_VERSION_MINIMUM=3.5

      - name: Build
        run: cmake --build build --config Release

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: RTSChess-x86_64
          path: build/RTSChess

  # ── Package universal .app ────────────────────────────────────────────────────
  package-macos:
    needs: [build-macos-arm64, build-macos-x86_64]
    runs-on: macos-latest
    steps:
      - name: Download arm64 binary
        uses: actions/download-artifact@v4
        with:
          name: RTSChess-arm64
          path: bins/arm64

      - name: Download x86_64 binary
        uses: actions/download-artifact@v4
        with:
          name: RTSChess-x86_64
          path: bins/x86_64

      - name: Create universal .app and zip
        run: |
          chmod +x bins/arm64/RTSChess bins/x86_64/RTSChess
          lipo -create bins/arm64/RTSChess bins/x86_64/RTSChess -output RTSChess-universal
          lipo -info RTSChess-universal
          mkdir -p RTSChess.app/Contents/MacOS
          mkdir -p RTSChess.app/Contents/Resources
          cp RTSChess-universal RTSChess.app/Contents/MacOS/RTSChess
          cat > RTSChess.app/Contents/Info.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>RTSChess</string>
              <key>CFBundleIdentifier</key>
              <string>com.jgashler.rtschess</string>
              <key>CFBundleName</key>
              <string>RTSChess</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundleVersion</key>
              <string>1</string>
              <key>NSHighResolutionCapable</key>
              <true/>
              <key>LSMinimumSystemVersion</key>
              <string>10.15</string>
          </dict>
          </plist>
          EOF
          codesign --force --deep -s - RTSChess.app
          zip -r RTSChess-macOS.zip RTSChess.app

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: RTSChess-macOS
          path: RTSChess-macOS.zip

  # ── Publish rolling release ───────────────────────────────────────────────────
  publish:
    needs: [build-windows, package-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: RTSChess-Windows
          path: dist/windows

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: RTSChess-macOS
          path: dist/macos

      - name: Rename for release
        run: |
          mv dist/windows/RTSChess.exe         dist/RTSChess-Windows.exe
          mv dist/macos/RTSChess-macOS.zip     dist/RTSChess-macOS.zip

      - name: Update rolling release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest-build
          name: RTSChess
          body: |
            RTSCHESS RTSCHESS RTSCHESS
          prerelease: false
          files: |
            dist/RTSChess-Windows.exe
            dist/RTSChess-macOS.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
